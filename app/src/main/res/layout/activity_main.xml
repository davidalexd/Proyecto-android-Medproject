<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:backgroundTintMode="src_atop"
    android:backgroundTint="#99000000"
    tools:context=".MainActivity"
    android:padding="30dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/main_logo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:src="@drawable/logo_lg"
        android:layout_marginTop="50dp"/>
    <Space
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"/>
    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="30sp"
        android:layout_below="@+id/main_logo"
        android:layout_marginVertical="20dp"
        android:fontFamily="@font/nunito_font"
        android:text="¡Los mejores  servicios para tu empresa!"/>
    <TextView
        android:id="@+id/desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="22sp"
        android:layout_below="@id/title"
        android:layout_marginVertical="20dp"
        android:text="MwedProyecto te ofrece servicios a bajo costo y alcance de tus manos"/>

    <Button
        android:id="@+id/btnStart"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@+id/desc"
        android:layout_gravity="bottom"
        android:background="@drawable/rounded_button"
        android:maxHeight="50dp"
        android:onClick="openGeneral"
        android:text="Entrar" />

</LinearLayout>